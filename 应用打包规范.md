# 应用打包规范

## 1. 应用标识(包名)

应用包名(appid)是应用的唯一标识，应用商店只接受使用**倒置域名**规则命名的应用。如:

```
org.deepin.browser
```

推荐使用你已经拥有的域名作为应用名称。如果使用未拥有的域名作为前缀，可能会引起该域名拥有者进行申诉，导致你的应用将会被删除。

**注意：此处的包名就是后续打包程序时生成的 control 文件的 Package 字段，仅支持小写字母。**

## 2. 应用安装

### 2.1 安装路径

应用的全部安装文件必须在如下目录(从源码编译的应用也需要将程序目录指定到如下目录)：

```
/opt/apps/${appid}/
```

**除对本程序根目录文件进行必要的操作修改外，禁止使用deb的postinst等钩子对系统文件进行修改，包含这些脚本的软件包都无法上架**

## 3. 目录结构

应用根目录下面有”entries” | “files”两个目录和一个”info”文件

```
org.deepin.browser/
├── entries`
├── files`
└── info`
```

### 3.1 entries目录

该目录所放置的资源文件夹在软件包安装后将自动映射到对应的系统目录中，主要放置”icons”和”applications”目录，
支持映射的目录可参考下表:

| **打包路径**                            | **映射路径**            | **备注**                                        |
| --------------------------------------- | ----------------------- | ----------------------------------------------- |
| /opt/apps/${appid}/entries/applications | /usr/share/applications | “desktop”启动方式文件                           |
| /opt/apps/${appid}/entries/doc          | /usr/share/doc          | 安装该软件包的商店版本 >= 7.6.2，该映射方可生效 |
| /opt/apps/${appid}/entries/fonts/files  | /usr/share/fonts        | 字体文件                                        |
| /opt/apps/${appid}/entries/help         | /usr/share/help         | 帮助手册文件                                    |
| /opt/apps/${appid}/entries/icons        | /usr/share/icons        | 图标文件                                        |
| /opt/apps/${appid}/entries/locale       | /usr/share/locale       | 翻译文件                                        |
| /opt/apps/${appid}/entries/mime         | /usr/share/mime         | mime数据库                                      |

#### 3.1.1 applications 目录

“applications”目录用于存放程序启动文件,即”xxx.desktop”文件，一般会有一个以 **“appid(包名)”** 命名的 desktop 文件放置在这个目录中。遵循该目录规范的deb安装包在dde桌面环境下安装后，程序的desktop文件（快捷方式）将会自动安装到启动器中。

**desktop文件编码必须为”UTF-8”，其他编码格式会导致中文乱码**

**示例：**

| **打包路径**                                    | **映射路径**                                       |
| ----------------------------------------------- | -------------------------------------------------- |
| entries/applications/org.deepin.browser.desktop | /usr/share/applications/org.deepin.browser.desktop |

目前使用标准的 desktop 格式，相关标准及其他字段可以参考

https://specifications.freedesktop.org/desktop-entry-spec/desktop-entry-spec-1.1.html

如果您应用的 **“ appid(包名) “** 为 **“ org.deepin.browser “** ，一个简单的例子，**“ org.deepin.browser.desktop “** 的基本内容如下:

```
[Desktop Entry]
Categories=Network;WebBrowser;
Name=Browser
Name[zh_CN]=浏览器
Keywords=deepin;uniontech;browser
Keywords[zh_CN]=深度;统信;浏览器
Comment=BAccess the Internet.
Comment[zh_CN]=访问互联网。
Exec=/opt/apps/org.deepin.browser/files/deepin
Icon=org.deepin.browser
Type=Application
Terminal=false
StartupWMClass=org.deepin.browser
StartupNotify=true
MimeType=audio/aac;application/aac;
```

**Desktop 文件中重点字段的解释：**

| **字段**        | **是否必填** | **说明**                                                     |
| --------------- | ------------ | ------------------------------------------------------------ |
| [Desktop Entry] | 必填         | desktop文件标识，标准格式，不需要改动。                      |
| Categories      | 选填         | 程序分类，用于标注程序所属类型。该字段不存在时，应用属于”其他应用”。 |
| Name            | 必填         | 程序在启动器中显示的通用名称，在没有单独设置语言参数的情况下，默认显示该名称。 |
| Keywords        | 选填         | 程序的通用关键搜索词，当在启动器中搜索该词而非程序名称时，即可索引出该程序的快捷方式。 |
| Comment         | 选填         | 关于本程序的通用简述，在没有单独设置语言参数的情况下，默认显示该段内容。 |
| Exec            | 必填         | 应用的运行程序所在路径，一般为程序的可执行二进制文件或启动脚本，后期会被沙箱启动。 **填写时需要关注路径的有效性，如果路径无效，在安装该程序后，程序快捷方式将不会显示在启动器中。相关参数可点击[参考](https://specifications.freedesktop.org/desktop-entry-spec/desktop-entry-spec-1.1.html#)** |
| Icon            | 必填         | 显示的图标所在路径，推荐使用相对名称以便于系统根据主题规范查找对应的图标文件，特殊情况下(显示异常等)需要使用绝对路径来获取图标。 |
| Type            | 必填         | 程序类型，必须为”Application”。                              |
| Terminal        | 必填         | 该字段决定程序是否以终端的形式运行，默认为”false”关闭状态，若该项为”true”开启状态，则应用将会在终端中打开。 |
| StartupNotify   | 必填         | 程序是否支持发送启动通知事件。为”true”时，允许桌面环境跟踪应用程序的启动，提供用户反馈和其他功能。例如鼠标的等待动画等。**为保障应用使用体检，该项建议填写”true”。** |
| MimeType        | 选填         | 程序支持的关联文件类型，根据实际需求来填写。                 |

Categories 应用类型可参考以下填写值来设置:

| 填写值      | 启动器中的分类 |
| ----------- | -------------- |
| Network     | 网络应用       |
| Chat        | 社交沟通       |
| Audio       | 音乐欣赏       |
| AudioVideo  | 视频播放       |
| Graphics    | 图形图像       |
| Game        | 游戏娱乐       |
| Office      | 办公学习       |
| Reading     | 阅读翻译       |
| Development | 编程开发       |
| System      | 系统管理       |

**除上述分类外，填写其他分类将会被归为”其他应用”**

#### 3.1.2 icons 目录

------

该目录用于放置应用icon图标，主要分为矢量格式和非矢量格式。

如果使用矢量格式图标，建议使用svg格式图标，路径设置为

```
entries/icons/hicolor/scalable/apps/org.deepin.browser.svg
```

如果使用非矢量格式，请按照 **分辨率** 来放置图标，如：

```
icons/hicolor/24x24/apps/org.deepin.browser.png

icons/hicolor/16x16/apps/org.deepin.browser.png
```

**注意：非矢量图标建议设置为 PNG 格式**

支持的分辨率包括:16/24/32/48/128/256/512

### 3.2 files目录

存放应用程序需要的各种文件，对于该目录放置文件并无限制，为方便管理，建议将可执行程序放置到bin子目录

### 3.3 info文件

info文件是应用的描述文件，用于dde桌面环境安装软件包时自动配置程序的安装文件，如 **“icons”** 和 **“desktop”** 文件。使用json格式，info内容如下：

```
{ 
    "appid": "org.deepin.browser",
     "name": "deepin",
     "version": "1.1.2",
     "arch": ["amd64","arm64"],
     "permissions": {
         "autostart": false,
         "notification": false,
         "trayicon": false,
         "clipboard": false,
         "account": false,
         "bluetooth": false,
         "camera": false,
         "audio_record": false,
         "installed_apps": false
   }
    "support-plugins": [
        "plugin/demo"
    ],
    "plugins": [
        "plugin/webbrowser",
        "plugin/office"
    ]
 }
```

**请修改填写完info文件后检查格式、字段以及字段值的内容是否符合要求，如有字段拼写错误、符号缺失、异常(如多余空格)等都有可能使info文件无效，导致应用在安装时dde桌面环境无法自动设置程序安装文件的目录关系**

info文件中各个字段的说明如下：

| 字段            | 说明                                                         |
| --------------- | ------------------------------------------------------------ |
| appid           | 应用标识(软件包名)                                           |
| name            | 应用默认名称                                                 |
| version         | 应用版本，格式为 {MAJOR}.{MINOR}.{PATCH}.{BUILD},所有版本号均为纯数字。此处填写的值应与**deb安装包**所标识的版本号一致 |
| arch            | 应用支持的架构，架构值需要使用半角双引号和半角中括号包含，且不同架构间使用半角逗号来分割。如仅支持amd64平台应该填写**[“amd64”]**；如同时支持amd64和arm64平台应填写**[“amd64”],[“arm64”]** |
| permissions     | 应用运行权限描述。                                           |
| support-plugins | 支持的插件类型                                               |
| plugins         | 实现的的插件类型，在对应的plugins目录下，按照实现的插件类型放置文件。在安装时，系统会将插件链接到对应的应用目录。 |

permissions 应用权限描述如下:

| 字段           | 说明                     | 可选值      |
| -------------- | ------------------------ | ----------- |
| autostart      | 是否允许自启动           | true/false  |
| notification   | 是否允许使用通知         | true/false  |
| trayicon       | 是否运行显示托盘图标     | true/false  |
| clipboard      | 是否允许使用剪切板       | true/false  |
| account        | 是否允许读取登录用户信息 | true/false  |
| bluetooth      | 是否允许使用蓝牙设备     | true/false  |
| camera         | 是否允许使用视频设备     | true/false  |
| audio_record   | 是否允许进行录音         | true/false  |
| installed_apps | 是否允许读取安装软件列表 | true/false  |

当前商店支持如下CPU架构:

| arch架构    | CPU系列                                             |
| ----------- | --------------------------------------------------- |
| amd64       | x86架构CPU:海光、兆芯、津逮、Intel、AMD             |
| arm64       | ARM64架构CPU:飞腾、鲲鹏、海思麒麟、鲲鹏、瑞芯微     |
| loongarch64 | LoongArch64架构龙芯CPU:3A5000、3B5000及更新的产品   |
| mips64el    | MIPS架构龙芯CPU:3A4000、3A3000以及更早型号的CPU产品 |
| sw_64       | 申威CPU                                             |

**开发者需要注意，应用只允许使用普通用户权限启动，禁止应用以任何形式获取root权限。**

support-plugins: 支持的插件类型

plugins：实现的的插件类型，在对应的plugins目录下，按照实现的插件类型放置文件。

在安装时，系统会将插件链接到对应的应用目录。

注意事项：

- desktop/serverice请指向自己的应用目录的/opt/apps/org.deepin.browser/files目录下的文件。
- 对于特殊的应用，有多个入口的desktop文件，表示功能不同的程序。对于这种需要有多个入口的程序，采用严格限制的白名单方式进行申请。一般应用程序不允许有多个入口的desktop文件。
- 在plugins机制中，plugins的每一个子目录以及主程序目录均是无法相互访问的，在开发过程中请尤其注意这一点。

## 4. 文件系统权限

### 4.1 系统目录

系统分区为只读状态，主要用于为应用提供基本的运行依赖库。请不要依据系统目录内容来做任何特性，后期系统将会将应用运行在沙箱中，系统目录中的内容将会都不可信。

### 4.2 应用数据目录

软件包不允许直接向$HOME目录直接写入文件，后期系统将会使用沙箱技术重新定向$HOME,任何依赖该特性的行为都可能失效。
应用使用如下环境变量指示的目录写入应用数据和配置：

```
$XDG_DATA_HOME
$XDG_CONFIG_HOME
$XDG_CACHE_HOME
```

对于appid为org.deepin.browser的应用，其写入目录为：

```
$XDG_DATA_HOME/org.deepin.browser
$XDG_CONFIG_HOME/org.deepin.browser
$XDG_CACHE_HOME/org.deepin.browser
```

### 4.3 用户数据目录

用户数据目录需要用户同意才能进行写入。

用户数据目录是定义在” ~/.config/user-dirs.dirs “中的路径，” ~/.config/user-dirs.dirs “内容如下：

```
cat ~/.config/user-dirs.dirs
XDG_DESKTOP_DIR="$HOME/Desktop"
XDG_DOCUMENTS_DIR="$HOME/Documents"
XDG_DOWNLOAD_DIR="$HOME/Downloads"
XDG_MUSIC_DIR="$HOME/Music"
XDG_PICTURES_DIR="$HOME/Pictures"
XDG_PUBLICSHARE_DIR="$HOME/Public"
XDG_TEMPLATES_DIR="$HOME/Templates"
XDG_VIDEOS_DIR="$HOME/Videos"
```

一般的UI框架如Qt提供了对数据目录的支持，可以优先考虑使用框架的接口来访问这些目录。
关于目录的定义，可以参考：
https://specifications.freedesktop.org/basedir-spec/basedir-spec-0.6.html

## 5. 包格式

### 5.1 二进制格式

统信UOS应用商店支持deb格式的二进制软件包，使用debuild工具进行打包。

## 6. deb 包钩子脚本

### 6.1 **`rm -rf` 命令使用规范**

在 deb 中的 DEBIAN 目录下如果存在 postinst、postrm、preinst、prerm 脚本，需要检查 rm -rf 的使用。

当 `rm -rf` 使用环境变量或外部输入时，**必须添加双引号**以防止路径解析错误：

```bash
# 正确做法（带引号）
rm -rf "$INSTALL_DIR/tmp"

# 错误做法（未引用的变量可能引发灾难性删除）
rm -rf $TEMP_PATH/*   # 若路径含空格或特殊字符，可能删除系统目录！
```

**最小化 `rm -rf` 使用范围**

避免删除用户数据或系统目录（如 `/var`、`/home`）。仅操作包自身文件。

**禁止递归删除根目录**

绝对禁用 `rm -rf /*`、`rm -rf /` 或 `rm -rf .*`！

### 6.2 强制检查项

**用 `shellcheck` 检查语法**

```bash
# 安装 shellcheck
sudo apt install shellcheck

# 检查所有钩子脚本
shellcheck DEBIAN/postinst DEBIAN/postrm DEBIAN/preinst DEBIAN/prerm
```